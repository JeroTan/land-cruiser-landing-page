## MODIFIED Requirements

### Requirement: Hero Section
The system MUST render a full-bleed Hero section at the top of the page featuring the Land Cruiser, a primary headline, and clear Calls to Action (CTAs). The image MUST fully cover the viewport area without leaving empty space.

#### Scenario: User views the page initially
- **WHEN** the user loads the landing page
- **THEN** the system displays the Hero section filling the viewport height with a dark background, a large headline (Toyota Type Bold), the product image covering the entire space (full-bleed), price, and two CTAs ("Buy" and "Learn more").